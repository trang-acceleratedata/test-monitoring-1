name: 'test_1'
version: '1.0.0'
config-version: 2

profile: 'test_1'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  test_1:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
